services:
  postgres:
    image: postgres:16
    container_name: postgres-local
    restart: unless-stopped
    ports:
      - "5432:5432"        # vystavený na localhost, app sa pripojí cez localhost
    environment:
      POSTGRES_DB: mydb
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword